# Memory Root

This directory is the agent's local memory surface.

- `data/` : structured datasets used by data skills (CSV/XLSX/XLS/Parquet)
- `notes/` : plain text notes and working documents
- `knowledge/` : long-lived reference material

All skill/tool actions must stay inside this tree.
