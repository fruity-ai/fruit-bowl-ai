# Images

`fruit-bowl-hero.svg` is the default README hero banner.

Guidelines:
- Keep image assets optimized for fast GitHub rendering.
- Prefer SVG for logos/illustrations and PNG for UI screenshots.
- Use relative paths from repository root in README references.
